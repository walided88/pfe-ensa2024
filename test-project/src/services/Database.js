export const addPlayer = (player) => {
    // Logique pour ajouter un joueur
  };
  
  export const getPlayers = () => {
    // Logique pour obtenir les joueurs
  };
  
  export const checkPlayerExists = (playerId) => {
    // Logique pour vérifier si un joueur existe
  };
  